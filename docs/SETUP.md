# Setup

## Prereqs
- Python 3.10+
- Snowflake account access
- TripleSeat API credentials

## Steps
1. Clone repo
2. Install dependencies: `pip install -r requirements.txt`
3. Create required credential JSON files (see README)
4. Run notebook / scripts
